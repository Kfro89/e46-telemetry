# DBC Files

Place your BMW E46 DBC here as `e46.dbc`, and point `config/config.yaml` to `./dbc/e46.dbc`.

You can also try:

```bash
../scripts/fetch_e46_dbc.sh
```

This will fetch a community E46 DBC into this folder. Validate signals on your specific car.
